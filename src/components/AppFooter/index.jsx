import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const AppFooter = () => <Footer style={{ textAlign: 'center' }}>Footer该写点什么</Footer>;

export default AppFooter;
